# Чеклист для тестирования OAuth (Steam и Discord)

## ✅ Обязательные переменные окружения

Убедитесь, что в `.env.local` есть все необходимые переменные:

```bash
# Supabase (обязательно)
NEXT_PUBLIC_SUPABASE_URL=https://vpzvmmsenskreznshevf.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Steam (опционально, для получения дополнительной информации о пользователе)
STEAM_API_KEY=your-steam-api-key
```

### Где найти ключи Supabase:

1. Перейдите в [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Settings** → **API**
4. Скопируйте:
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** key → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role** key → `SUPABASE_SERVICE_ROLE_KEY` (⚠️ **НЕ ПУБЛИКУЙТЕ ЭТОТ КЛЮЧ!**)

### Где найти Steam API Key (опционально):

1. Перейдите на https://steamcommunity.com/dev/apikey
2. Войдите в свой Steam аккаунт
3. При создании API ключа:
   - **Domain/Website**: можно указать `http://localhost:3000` для разработки или оставить пустым (если поле необязательное)
   - Для продакшена позже обновите на ваш реальный домен
4. Создайте новый API ключ
5. Добавьте в `.env.local`: `STEAM_API_KEY=your-key`

**Важно:** Steam OpenID работает с localhost без проблем, домен нужен только для Steam Web API (опционально, для получения дополнительной информации о пользователе).

---

## 🔵 Настройка Discord OAuth

### Шаг 1: Создание Discord приложения

1. Перейдите на https://discord.com/developers/applications
2. Нажмите **"New Application"**
3. Введите название приложения (например, "Squad Invasion")
4. Нажмите **"Create"**

### Шаг 2: Настройка OAuth2 в Discord

1. В меню слева выберите **"OAuth2"**
2. В разделе **"Redirects"** нажмите **"Add Redirect"**
3. Добавьте URL (замените на ваш Supabase URL):
   ```
   https://vpzvmmsenskreznshevf.supabase.co/auth/v1/callback
   ```
4. Нажмите **"Save Changes"**
5. Скопируйте:
   - **Client ID** (в разделе "OAuth2" → "General")
   - **Client Secret** (нажмите "Reset Secret" если нужно, затем скопируйте)

### Шаг 3: Настройка в Supabase Dashboard

1. Перейдите в [Supabase Dashboard](https://app.supabase.com) → ваш проект
2. Перейдите в **Authentication** → **Providers**
3. Найдите **"Discord"** в списке
4. Включите провайдер (переключите тумблер)
5. Введите:
   - **Client ID**: из Discord Developer Portal
   - **Client Secret**: из Discord Developer Portal
6. Нажмите **"Save"**

### Шаг 4: Настройка Redirect URLs в Supabase

1. В Supabase Dashboard перейдите в **Authentication** → **URL Configuration**
2. Убедитесь, что **Site URL** установлен:
   ```
   http://localhost:3000
   ```
3. В разделе **Redirect URLs** добавьте (если их еще нет):
   ```
   http://localhost:3000/ru/auth/callback
   http://localhost:3000/en/auth/callback
   ```
4. Для продакшена добавьте также:
   ```
   https://yourdomain.com/ru/auth/callback
   https://yourdomain.com/en/auth/callback
   ```
5. Нажмите **"Save"**

---

## 🔴 Настройка Google OAuth

### Шаг 1: Создание проекта в Google Cloud Console

1. Перейдите на https://console.cloud.google.com
2. Создайте новый проект или выберите существующий
3. Введите название проекта (например, "Squad Invasion")
4. Нажмите **"Create"**

### Шаг 2: Настройка OAuth Consent Screen

1. В меню слева выберите **"APIs & Services"** → **"OAuth consent screen"**
2. Выберите тип пользователей:
   - **Internal** - только для пользователей вашей организации (требует Google Workspace)
   - **External** - для всех пользователей Google (рекомендуется)
3. Заполните обязательные поля:
   - **App name**: название вашего приложения
   - **User support email**: ваш email для поддержки
   - **Developer contact information**: ваш email
4. Нажмите **"Save and Continue"**
5. На странице **Scopes** нажмите **"Add or Remove Scopes"**
6. Добавьте scopes:
   - `email`
   - `profile`
7. Нажмите **"Update"**, затем **"Save and Continue"**
8. На странице **Test users** (если выбрали External) добавьте тестовых пользователей (опционально)
9. Нажмите **"Save and Continue"** → **"Back to Dashboard"**

### Шаг 3: Создание OAuth 2.0 Credentials

1. В меню слева выберите **"APIs & Services"** → **"Credentials"**
2. Нажмите **"Create Credentials"** → **"OAuth client ID"**
3. Выберите **Application type**: **Web application**
4. Введите **Name**: например, "Squad Invasion Web"
5. В разделе **Authorized redirect URIs** нажмите **"Add URI"** и добавьте:
   ```
   https://vpzvmmsenskreznshevf.supabase.co/auth/v1/callback
   ```
   (Замените на ваш Supabase URL)
6. Нажмите **"Create"**
7. Скопируйте **Client ID** и **Client Secret** (они появятся в модальном окне)

### Шаг 4: Настройка в Supabase Dashboard

1. Перейдите в [Supabase Dashboard](https://app.supabase.com) → ваш проект
2. Перейдите в **Authentication** → **Providers**
3. Найдите **"Google"** в списке
4. Включите провайдер (переключите тумблер)
5. Введите:
   - **Client ID**: из Google Cloud Console
   - **Client Secret**: из Google Cloud Console
6. Нажмите **"Save"**

### Шаг 5: Настройка Redirect URLs в Supabase

1. В Supabase Dashboard перейдите в **Authentication** → **URL Configuration**
2. Убедитесь, что **Site URL** установлен:
   ```
   http://localhost:3000
   ```
3. В разделе **Redirect URLs** добавьте (если их еще нет):
   ```
   http://localhost:3000/ru/auth/callback
   http://localhost:3000/en/auth/callback
   ```
4. Для продакшена добавьте также:
   ```
   https://yourdomain.com/ru/auth/callback
   https://yourdomain.com/en/auth/callback
   ```
5. Нажмите **"Save"**

---

## 🟠 Настройка Steam OAuth

**Хорошие новости:** Steam не требует настройки в Supabase Dashboard! Он работает через кастомный OpenID flow.

### Что нужно проверить:

1. ✅ Убедитесь, что переменные окружения настроены (см. выше)
2. ✅ Steam API Key опционален (нужен только для получения дополнительной информации о пользователе)
3. ✅ **Домен не требуется для разработки** - Steam OpenID работает с `localhost` без проблем

### Про домен в Steam Web API Key:

- Если при регистрации Steam Web API Key запрашивается домен:
  - Для разработки: укажите `http://localhost:3000` или оставьте пустым (если поле необязательное)
  - Для продакшена: позже обновите на ваш реальный домен
- **Важно:** Steam OpenID не требует предварительной регистрации домена - он работает с любым доменом, включая localhost

### Как работает Steam OAuth:

- Пользователь нажимает "Войти через Steam"
- Перенаправляется на Steam для авторизации
- Steam возвращает на `/api/auth/steam/callback`
- Система автоматически создает пользователя в Supabase Auth и таблице `users`
- Пользователь автоматически входит в систему

---

## 🧪 Тестирование

### Перед тестированием:

1. ✅ Убедитесь, что все переменные окружения настроены
2. ✅ Discord приложение создано и настроено
3. ✅ Discord провайдер включен в Supabase Dashboard
4. ✅ Redirect URLs настроены в Supabase Dashboard
5. ✅ Запустите dev сервер: `npm run dev`

### Тестирование Discord:

1. Откройте http://localhost:3000
2. Нажмите кнопку **"Войти"** в хедере
3. В модальном окне нажмите **"Войти через Discord"**
4. Вы должны быть перенаправлены на Discord для авторизации
5. После авторизации вы вернетесь на сайт
6. Проверьте, что вы вошли в систему

### Тестирование Steam:

1. Откройте http://localhost:3000
2. Нажмите кнопку **"Войти"** в хедере
3. В модальном окне нажмите **"Войти через Steam"**
4. Вы должны быть перенаправлены на Steam для авторизации
5. После авторизации вы вернетесь на сайт
6. Проверьте, что вы вошли в систему

### Тестирование Google:

1. Откройте http://localhost:3000
2. Нажмите кнопку **"Войти"** в хедере
3. В модальном окне нажмите **"Войти через Google"**
4. Вы должны быть перенаправлены на Google для авторизации
5. Выберите аккаунт Google и разрешите доступ
6. После авторизации вы вернетесь на сайт
7. Проверьте, что вы вошли в систему

### Проверка в Supabase Dashboard:

1. Перейдите в **Authentication** → **Users**
2. Вы должны увидеть созданного пользователя
3. Проверьте, что в `user_metadata` есть информация о провайдере

### Проверка в базе данных:

1. Перейдите в **Table Editor** → **users**
2. Вы должны увидеть запись с вашим пользователем
3. Проверьте, что `steam_id` или `discord_id` заполнены

---

## ❌ Решение проблем

### Discord: "Invalid redirect_uri"

- Убедитесь, что redirect URL в Discord Developer Portal точно совпадает с URL в Supabase
- URL должен быть: `https://vpzvmmsenskreznshevf.supabase.co/auth/v1/callback`

### Discord: "OAuth2 application not found"

- Проверьте, что Client ID и Client Secret правильно скопированы
- Убедитесь, что Discord приложение не удалено

### Steam: "Steam authentication failed"

- Проверьте, что `SUPABASE_SERVICE_ROLE_KEY` правильно настроен
- Проверьте консоль сервера на наличие ошибок

### Google: "OAuth2 application not found"

- Проверьте, что Client ID и Client Secret правильно скопированы
- Убедитесь, что OAuth consent screen настроен
- Проверьте, что redirect URI в Google Cloud Console совпадает с Supabase callback URL

### Google: "redirect_uri_mismatch"

- Убедитесь, что redirect URL в Google Cloud Console точно совпадает с URL в Supabase
- URL должен быть: `https://vpzvmmsenskreznshevf.supabase.co/auth/v1/callback`

### Общие проблемы:

- **"Missing environment variables"**: Проверьте `.env.local` файл
- **"CORS error"**: Убедитесь, что Site URL правильно настроен в Supabase Dashboard
- **"User not created"**: Проверьте, что таблица `users` существует и миграции применены

---

## 📝 Следующие шаги

После успешного тестирования:

1. ✅ Настройте продакшен redirect URLs в Supabase Dashboard
2. ✅ Добавьте переменные окружения в Vercel (для продакшена)
3. ✅ Обновите Discord redirect URL для продакшена домена
4. ✅ Протестируйте на продакшене

---

## 🔒 Безопасность

⚠️ **ВАЖНО:**

- **НИКОГДА** не коммитьте `.env.local` в Git
- **НИКОГДА** не публикуйте `SUPABASE_SERVICE_ROLE_KEY` публично
- Используйте разные Discord приложения для dev и production
- Регулярно обновляйте Client Secret в Discord
